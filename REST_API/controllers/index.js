const authController = require('./auth');
const productController = require('./productController');
const commentsController = require('./commentsControlls')

module.exports = {
    authController,
    productController,
    commentsController
}